package com.example.basekotlin.model

enum class EnumGesture {
    SINGLE, SWIPE_LEFT, SWIPE_RIGHT, LONG_PRESS
}

enum class EnumColor {
    ICON, BACKGROUND
}

enum class EnumStatusColor {
    WHITE, BACKGROUND
}